// This file was auto-generated by @joelek/ts-autoguard. Edit at own risk.

export type FieldOrder = ("bff" | "tff" | "progressive");

export const FieldOrder = {
	as(subject: any, path: string = ""): FieldOrder {
		return ((subject, path) => {
			try {
				return ((subject, path) => {
					if (subject === "bff") {
						return subject;
					}
					throw "Type guard \"StringLiteral\" failed at \"" + path + "\"!";
				})(subject, path);
			} catch (error) {}
			try {
				return ((subject, path) => {
					if (subject === "tff") {
						return subject;
					}
					throw "Type guard \"StringLiteral\" failed at \"" + path + "\"!";
				})(subject, path);
			} catch (error) {}
			try {
				return ((subject, path) => {
					if (subject === "progressive") {
						return subject;
					}
					throw "Type guard \"StringLiteral\" failed at \"" + path + "\"!";
				})(subject, path);
			} catch (error) {}
			throw "Type guard \"Union\" failed at \"" + path + "\"!";
		})(subject, path);
	},
	is(subject: any): subject is FieldOrder {
		try {
			FieldOrder.as(subject);
		} catch (error) {
			return false;
		}
		return true;
	}
};

export type PictureSettings = {
	dimx: number,
	dimy: number,
	parx: number,
	pary: number,
	darx: number,
	dary: number,
	farx: number,
	fary: number,
	fpsx: number,
	fpsy: number,
	color_range: (string | null),
	color_space: (string | null),
	color_transfer: (string | null),
	color_primaries: (string | null),
	aspect_filter: string
};

export const PictureSettings = {
	as(subject: any, path: string = ""): PictureSettings {
		return ((subject, path) => {
			if ((subject != null) && (subject.constructor === Object)) {
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.dimx, path + "." + "dimx");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.dimy, path + "." + "dimy");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.parx, path + "." + "parx");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.pary, path + "." + "pary");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.darx, path + "." + "darx");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.dary, path + "." + "dary");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.farx, path + "." + "farx");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.fary, path + "." + "fary");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.fpsx, path + "." + "fpsx");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.fpsy, path + "." + "fpsy");
				((subject, path) => {
					try {
						return ((subject, path) => {
							if ((subject != null) && (subject.constructor === String)) {
								return subject;
							}
							throw "Type guard \"String\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					try {
						return ((subject, path) => {
							if (subject === null) {
								return subject;
							}
							throw "Type guard \"Null\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					throw "Type guard \"Union\" failed at \"" + path + "\"!";
				})(subject.color_range, path + "." + "color_range");
				((subject, path) => {
					try {
						return ((subject, path) => {
							if ((subject != null) && (subject.constructor === String)) {
								return subject;
							}
							throw "Type guard \"String\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					try {
						return ((subject, path) => {
							if (subject === null) {
								return subject;
							}
							throw "Type guard \"Null\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					throw "Type guard \"Union\" failed at \"" + path + "\"!";
				})(subject.color_space, path + "." + "color_space");
				((subject, path) => {
					try {
						return ((subject, path) => {
							if ((subject != null) && (subject.constructor === String)) {
								return subject;
							}
							throw "Type guard \"String\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					try {
						return ((subject, path) => {
							if (subject === null) {
								return subject;
							}
							throw "Type guard \"Null\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					throw "Type guard \"Union\" failed at \"" + path + "\"!";
				})(subject.color_transfer, path + "." + "color_transfer");
				((subject, path) => {
					try {
						return ((subject, path) => {
							if ((subject != null) && (subject.constructor === String)) {
								return subject;
							}
							throw "Type guard \"String\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					try {
						return ((subject, path) => {
							if (subject === null) {
								return subject;
							}
							throw "Type guard \"Null\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					throw "Type guard \"Union\" failed at \"" + path + "\"!";
				})(subject.color_primaries, path + "." + "color_primaries");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === String)) {
						return subject;
					}
					throw "Type guard \"String\" failed at \"" + path + "\"!";
				})(subject.aspect_filter, path + "." + "aspect_filter");
				return subject;
			}
			throw "Type guard \"Object\" failed at \"" + path + "\"!";
		})(subject, path);
	},
	is(subject: any): subject is PictureSettings {
		try {
			PictureSettings.as(subject);
		} catch (error) {
			return false;
		}
		return true;
	}
};

export type RectSettings = {
	w: number,
	h: number,
	x: number,
	y: number,
	darx: number,
	dary: number
};

export const RectSettings = {
	as(subject: any, path: string = ""): RectSettings {
		return ((subject, path) => {
			if ((subject != null) && (subject.constructor === Object)) {
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.w, path + "." + "w");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.h, path + "." + "h");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.x, path + "." + "x");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.y, path + "." + "y");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.darx, path + "." + "darx");
				((subject, path) => {
					if ((subject != null) && (subject.constructor === Number)) {
						return subject;
					}
					throw "Type guard \"Number\" failed at \"" + path + "\"!";
				})(subject.dary, path + "." + "dary");
				return subject;
			}
			throw "Type guard \"Object\" failed at \"" + path + "\"!";
		})(subject, path);
	},
	is(subject: any): subject is RectSettings {
		try {
			RectSettings.as(subject);
		} catch (error) {
			return false;
		}
		return true;
	}
};

export type Settings = {
	picture: PictureSettings,
	rect: RectSettings,
	imode: FieldOrder,
	compressibility: (number | undefined)
};

export const Settings = {
	as(subject: any, path: string = ""): Settings {
		return ((subject, path) => {
			if ((subject != null) && (subject.constructor === Object)) {
				(PictureSettings.as)(subject.picture, path + "." + "picture");
				(RectSettings.as)(subject.rect, path + "." + "rect");
				(FieldOrder.as)(subject.imode, path + "." + "imode");
				((subject, path) => {
					try {
						return ((subject, path) => {
							if ((subject != null) && (subject.constructor === Number)) {
								return subject;
							}
							throw "Type guard \"Number\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					try {
						return ((subject, path) => {
							if (subject === undefined) {
								return subject;
							}
							throw "Type guard \"Undefined\" failed at \"" + path + "\"!";
						})(subject, path);
					} catch (error) {}
					throw "Type guard \"Union\" failed at \"" + path + "\"!";
				})(subject.compressibility, path + "." + "compressibility");
				return subject;
			}
			throw "Type guard \"Object\" failed at \"" + path + "\"!";
		})(subject, path);
	},
	is(subject: any): subject is Settings {
		try {
			Settings.as(subject);
		} catch (error) {
			return false;
		}
		return true;
	}
};

export type SettingsDatabase = { [key: string]: Settings };

export const SettingsDatabase = {
	as(subject: any, path: string = ""): SettingsDatabase {
		return ((subject, path) => {
			if ((subject != null) && (subject.constructor === Object)) {
				for (let key of Object.keys(subject)) {
					(Settings.as)(subject[key], path + "[\"" + key + "\"]");
				}
				return subject;
			}
			throw "Type guard \"Record\" failed at \"" + path + "\"!";
		})(subject, path);
	},
	is(subject: any): subject is SettingsDatabase {
		try {
			SettingsDatabase.as(subject);
		} catch (error) {
			return false;
		}
		return true;
	}
};

export type Autoguard = {
	FieldOrder: FieldOrder,
	PictureSettings: PictureSettings,
	RectSettings: RectSettings,
	Settings: Settings,
	SettingsDatabase: SettingsDatabase
};

export const Autoguard = {
	FieldOrder: FieldOrder,
	PictureSettings: PictureSettings,
	RectSettings: RectSettings,
	Settings: Settings,
	SettingsDatabase: SettingsDatabase
};
